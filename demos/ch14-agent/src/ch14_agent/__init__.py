"""ch14-agent — toolset design demo for Ch14.

Public modules:
- state: per-run identifiers (Lambda role/name, Gateway name, Runtime name)
- tools: the Strands @tool wrappers (query_tickets, lookup_alarm_code)
- agent: Strands Agent factory + system prompt
"""
